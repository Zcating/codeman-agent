# V2.1 Baseline — 2026-06-27

Captured by T0.1 (e2e run), T0.2 (drift audit), T0.3 (pi-mono pinning).

## Section 1 — e2e Baseline (T0.1)

Run command: `vp run e2e 2>&1 | Tee-Object e2e-baseline.log`
Wall-clock: 4.8m
Total tests: **16 tests across 10 spec files** (not 10 total as plan agent ground-truth said; some files have multiple tests).

| Status | Spec | Test name |
|---|---|---|
| ✅ pass | 01-app-launch | 主窗口加载聊天布局 |
| ✅ pass | 02-settings-api-key | 设置、持久化并重新加载 — key 被写入但永不反射 |
| ❌ **fail** | 04-llm-stream | 发送消息并在 30s 内观察到非空 assistant 文本或 Cancel 按钮 |
| ✅ pass | 04-theme-toggle | light → dark → light 通过 update_settings |
| ✅ pass | 05-chat-message-bubble | 输入内容产生可见用户气泡并持久化到 DB |
| ✅ pass | 05-chat-message-bubble | 多次发送产生多个气泡,无去重问题 |
| ✅ pass | 05-file-tools | edit_file: old_text 匹配多次时报错;精确匹配时成功 |
| ✅ pass | 05-file-tools | search_files 返回匹配文件 + 行号,不包含无关文件 |
| ✅ pass | 06-llm-round-trip | 正常输入 + 错 API Key → 1 user + 1 assistant = 2 bubble |
| ✅ pass | 07-mock-provider | 纯文本响应: assistant bubble 包含预置的固定文本 |
| ✅ pass | 08-file-tools-mock | write_file + read_file: 写文件后能读回内容 |
| ✅ pass | 08-file-tools-mock | 沙箱越界: read_file 越界时返回 SandboxViolation |
| ✅ pass | 09-per-conv-runtime | D1+D3: A streaming 不 leak 到 B view; 切回 A 内容完整 |
| ✅ pass | 09-per-conv-runtime | D5: sidebar streaming 指示 (⏳ 出现) streaming conv,完成后消失 |
| ✅ pass | 09-per-conv-runtime | D2: Cancel 中断 in-flight; Send 按钮恢复; 新 send 正常工作 |
| ✅ pass | 09-per-conv-runtime | D1+D3+D5: 2 个 conv 同时 streaming,sidebar 各自显示 ⏳ |

**Result: 15 passed / 1 failed.**

### Known failure analysis (V2.2 backlog)

`e2e/04-llm-stream.spec.ts:82` — LLM stream not observed within 30s. This is **NOT a regression** introduced by V2.1 (we haven't started implementation). This is a pre-existing V2 tech debt already documented in (queue-based runtime, e2e verification gap).

**V2.1 must NOT fix this**. Recorded as V2.2 backlog.

## Section 2 — Drift Audit (T0.2)

AGENTS.md / CONTEXT.md claim files that DON'T exist on disk:

| File | Claimed by | Action |
|---|---|---|
| `src/shared/lib/design-tokens.ts` | `src/shared/AGENTS.md` row 9 | Build in Phase 2 (T2.2) |
| `src/shared/lib/design-tokens.test.ts` | implicit in row 9 | Build in Phase 2 (T2.3) |
| `src/shared/components/ui/sidebar.tsx` | `src/shared/AGENTS.md` row 12 | Build in Phase 2 (T2.4) |
| `src/shared/components/ui/sidebar.test.tsx` | implicit | Build in Phase 2 (T2.5) |
| `src/shared/components/internal/agent-sidebar.tsx` | `src/shared/AGENTS.md` row 13 | Build in Phase 3 (T3.1) |
| `src/shared/components/internal/agent-sidebar.test.tsx` | implicit | Build in Phase 3 (T3.1) |
| `src/features/chat/components/home.tsx` | `src/features/chat/AGENTS.md` tree | Build in Phase 4 (T4.1) |
| `src/features/chat/components/home.test.tsx` | implicit | Build in Phase 4 (T4.1) |

**8 drift entries** — all expected to be created during V2.1 phases. Drift gate (Phase 7 T7.4) verifies closure.

**Additional drift (caught by T0.2):**

- `src/test-setup.ts` — original `src/AGENTS.md` table claimed exists; **does NOT exist**. Original docs may have been wrong about this path or it was removed but not updated in AGENTS.md. Need to check actual setup file location.

## Section 3 — pi-mono Pinning (T0.3)

| Package | Before | After |
|---|---|---|
| `@mariozechner/pi-agent` | `"latest"` | `"0.9.0"` (exact pin from node_modules) |
| `@mariozechner/pi-ai` | `"0.9.4"` | unchanged (already pinned) |

**Future upgrades**: pin must be changed via hyperplan review, not ad-hoc `pnpm update`.

## Section 4 — Scope Acknowledgment (per H-F2)

V2.1 plan explicitly excludes:

- ❌ Fix e2e/04-llm-stream.spec.ts failure (V2.2 backlog, root cause)
- ❌ Sidebar collapse toggle (A-F4) — V2.2
- ❌ Glass morphism / ambient pulse / progressive bubble density / new message flash (A-F7–A-F10) — V2.2
- ❌ Billing tool stale + cache (H-F3) — V2.2
- ❌ Workspace-deleted orphan convs (H-F6) — V2.2
- ❌ Other e2e gaps (file-tools round-trip / mock-provider beyond what's already passing) — V2.2

V2.1 scope is **strictly**: Codex-like Home + Sidebar重构  + 图标统一 + Doc sync.

---

## Section 5 — Drift Gate Verification (T7.4)

### Files MUST exist (claimed in AGENTS.md, created during V2.1)

| File | Status |
|---|---|
| `src/shared/lib/design-tokens.ts` | ✓ exists |
| `src/shared/lib/design-tokens.test.ts` | ✓ exists |
| `src/shared/components/ui/sidebar.tsx` | ✓ exists |
| `src/shared/components/ui/sidebar.test.tsx` | ✓ exists |
| `src/shared/components/internal/agent-sidebar.tsx` | ✓ exists |
| `src/shared/components/internal/agent-sidebar.test.tsx` | ✓ exists |
| `src/features/chat/components/home.tsx` | ✓ exists |
| `src/features/chat/components/home.test.tsx` | ✓ exists |
| `src/features/chat/routes/index.test.tsx` | ✓ exists |

### Files MUST NOT exist (deleted during V2.1)

| File | Status |
|---|---|
| `src/features/chat/components/sidebar.tsx` | ✓ deleted (T3.3) |
| `src/features/chat/components/sidebar.test.tsx` | ✓ deleted (T3.3) |

### Doc references consistency

| Check | Status |
|---|---|
| `src/test-setup.ts` references in docs | ✓ all replaced with `vitest.setup.ts` (T7.1) |

### Drift gate status: **PASSED**

V2.1 implementation now consistent with documentation. All claimed files exist; deleted files no longer in working tree; no stale doc references.