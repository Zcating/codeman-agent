# Ultrawork Notepad — V2.1 codeman-agent 改造

Started: 2026-06-27
Mode: subagent-driven (per plan agent recommendation)
Plan source: docs/plans/2026-06-27-v2.1-home-agent.md (will be written by T0.x)

## Plan (exhaustive, atomic)

### Phase 0 — Preflight (Wave 0, parallel)
- T0.1: e2e baseline (`vp run e2e`, capture log)
- T0.2: drift audit (AGENTS.md claims vs disk)
- T0.3: pin pi-mono deps to exact versions in package.json

### Phase 1 — Backend Data Layer (Wave 1, strict serialization)
- T1.1: Rust migration `0002_conversation_workspace.sql` (ALTER TABLE + index)
- T1.2: Rust `Conversation` struct + `create_conversation` IPC
- T1.3: Rust `Settings.last_used_workspace_id` field + sanitized()
- T1.4: TS `Conversation.workspace_id: string`
- T1.5: TS `Settings.last_used_workspace_id?: string`
- T1.6: TS `appStore.setLastUsedWorkspaceId` / `getLastUsedWorkspaceId`
- T1.7: TS `selectedWorkspaceId` derived getter
- T1.8: TS `createConversation(workspaceId, title, systemPrompt?)` signature + mock update
- T1.9: TS mockState IPC mock update

### Phase 2 — Design Tokens + Sidebar UI Primitive (Wave 2)
- T2.1: CSS `@theme` 8 `--color-sidebar*` tokens
- T2.2: TS `design-tokens.ts` (SIDEBAR_TRANSITION_MS, SIDEBAR_KEYBOARD_SHORTCUT)
- T2.3: TS `design-tokens.test.ts`
- T2.4: TS `ui/sidebar.tsx` primitive (13+ sub-components)
- T2.5: TS `ui/sidebar.test.tsx`

### Phase 3 — Agent Sidebar (Wave 3)
- T3.1: TS `internal/agent-sidebar.tsx` + test (prop-driven)
- T3.3: DELETE `features/chat/components/sidebar.tsx` + test

### Phase 4 — Home + Routes (Wave 4)
- T4.1: TS `home.tsx` + test (CodexForm + WorkspacePicker; 0/1/2+ state machine)
- T4.2: TS `createAndSendConversation` in `conversations.store.ts`
- T4.3: TS `routes/index.tsx` state machine rewrite + test

### Phase 5 — Icon Unification (Wave 5, 4 parallel tracks)
- T5.1: `tool-call-card.tsx` emoji → lucide (Loader2 / CheckCircle2 / XCircle)
- T5.2: `message-bubble.tsx` emoji → lucide
- T5.3: `chat-view.tsx` thinking indicator (Loader2 with NO animate-spin, border-l-2)
- T5.4: `workspace-card.tsx` Browse icon → FolderOpen (added per session decision)

### Phase 6 — E2E (Wave 6)
- T6.1: `e2e/helpers.ts` add `clickHomeAndWait`, `submitCodexForm`
- T6.2: `e2e/10-home-agent.spec.ts` (3 tests: 2 ws / 0 ws / submit form)
- T6.3: Migrate specs 01-09 to use new helpers

### Phase 7 — Doc Sync (Wave 7)
- T7.1: `src/AGENTS.md` fix paths + tree + lookup guide
- T7.2: `src/shared/AGENTS.md` update lib/ + components/internal/ rows
- T7.3: `src/features/chat/AGENTS.md` add test-coverage hard rule + replace Sidebar rule
- T7.4: Drift gate verify (all claimed files exist)

## Scenarios (the contract)

| Class | Scenario | Pass condition | Surface evidence |
|---|---|---|---|
| Happy | App launches → Home (no conv) | Home component visible, sidebar visible | tauri dev + screenshot |
| Happy | Home with 1 workspace → input enabled | Input editable, send button enabled | tauri dev + interact |
| Happy | Home with 2+ workspaces → input disabled until pick | Input disabled; click card → input enabled | tauri dev + interact |
| Happy | Submit form → ChatView + LLM stream | New conv in DB, activeId set, LLM streaming | e2e + DB inspect |
| Edge | 0 workspaces → CTA to /settings | Input disabled, CTA visible, navigates to /settings | tauri dev + interact |
| Edge | Sidebar collapse (V2.2 backlog, skipped) | — | — |
| Adjacent | Existing e2e specs 01-09 still pass after migration | No new failures vs baseline | `vp run e2e` |
| Adjacent | All claimed files in AGENTS.md actually exist | Drift gate closed (Phase 7) | grep + file exists |

## Now

Wave 0 — Preflight, dispatch 3 parallel tasks.

## Todo (remaining, ordered)

(Updated as work progresses)

## Findings (non-obvious facts with file:line refs)

- references `docs/adr/0022-internal-components-and-design-tokens.md`
- `appStore` is at `src/shared/stores/app.store.ts` (NOT `features/chat/stores/`)
- `Settings` type at `src/shared/lib/types.ts`
- 4 docs/plans path: `docs/plans/2026-06-27-v2.1-home-agent.md`
- `update_settings` Rust IPC is full overwrite (NOT partial patch) — TS-side merge in `appStore.set()`
- e2e spec count: 10 actual (2 `test.skip`), NOT 16 (per plan agent ground-truth check)
- AGENTS.md had premature file-existence claims (D-F6 drift bug) — Phase 7 fixes
- Plan agent proposed subagent-driven execution (recommended); I'll follow
- V2.2 backlog (explicit): sidebar collapse toggle, glass morphism, ambient pulse, progressive bubble density, new message flash, billing stale/cache, workspace-deleted orphan handling, e2e failures

## Learnings (patterns / pitfalls for next turn)

- Tailwind JIT scan: TS const in template literal `w-[${X}]` is NOT scanned; use CSS variables for layout
- animate-spin in thinking indicator: violates "calm/professional" product constraint → use border-l-2 static
- "无 conversations.store.ts 神超大文件" finding was refined to "test coverage constraint per exported sub-function"
- internal/ value = pattern investment, not current consumer count
- Doc/Impl sync gate must close at end (Phase 7 T7.4)